#!/bin/bash
mysqldump -u root -p<%= @settings['mysql_root_pwd'] %> --ignore-table mysql.event --ignore-table mysql.user  --all-databases | gzip -9 | ssh root@managesf.<%= scope.function_hiera(["top_domain"]) %> > /root/alldb.sql.gz
