---
prelude: >
    This is a minor release to fix incorrect 2.1.5 build release
    (the .tgz file is actually from 2.1.4)
features:
  - Use ansible to update known_hosts
issues:
  - Fix publish script to remove previous edeploy image
  - Fix gearman service to zuul.fqdn
others:
  - Fix swift artifact upload integration tests
