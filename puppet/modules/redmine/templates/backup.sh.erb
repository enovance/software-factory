#!/bin/bash
exec > /dev/null
mysqldump --ignore-table mysql.event \
          --ignore-table mysql.user  \
          --databases redmine | \
            gzip -9 | ssh root@managesf.<%= @fqdn %> tee /root/redminedb.sql.gz
