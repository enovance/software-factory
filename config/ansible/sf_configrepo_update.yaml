---
# hosts: gerrit:  init firstuser, set defaultacl, ...
- hosts: jenkins
  gather_facts: no
  tasks:
    - include: tasks/jenkins_update.yaml
- hosts: zuul
  gather_facts: no
  tasks:
    - include: tasks/zuul_update.yaml
- hosts: nodepool
  gather_facts: no
  tasks:
    - include: tasks/nodepool_update.yaml
