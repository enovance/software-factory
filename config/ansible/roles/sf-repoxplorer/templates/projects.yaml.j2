templates:
- name: default
  branch: master
  gitweb: https://{{ fqdn }}/r/gitweb?p=%(name)s.git;a=commit;h=%%(sha)s
  uri: https://{{ fqdn }}/r/%(name)s
projects:
  {{ fqdn }}-config:
    - name: config
      template: default
