#!/bin/bash
exec > /dev/null
mysqldump --ignore-table mysql.event \
          --ignore-table mysql.user  \
          --databases etherpad | \
            gzip -9 | ssh root@managesf.<%= @fqdn %> tee /root/etherpaddb.sql.gz
