input {
  file {
    path => "/var/lib/jenkins/jobs/*/*/*/log"
    start_position => "beginning"
    type => "jenkins_tests"
    }
}

filter {
}

output {
  elasticsearch {
    host => "localhost:9200"
  }
}