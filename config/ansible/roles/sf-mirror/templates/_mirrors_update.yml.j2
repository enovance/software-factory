# This file is managed by sfconfig.sh, don't edit.

- job:
    name: 'sf_mirror-update'
    defaults: global
    builders:
      - shell: |
          echo "Updating configuration using $ZUUL_COMMIT"
          ssh root@managesf.{{fqdn}} /usr/local/bin/sf-config-update.sh sf_mirror_update
    triggers:
      - zuul
    node: master
